import Content from '../contentsData/Content';
import TopContainer from "../bodyContainer/TopContainer";

function Daiv() {
  return (
    <TopContainer>
        <Content></Content>
    </TopContainer>
  );
}

export default Daiv;
